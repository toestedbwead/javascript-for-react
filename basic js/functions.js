let name = "Levi";
const age = 21;

function greet(name) {
    return "Hello " + name;
}

const greet = (name) => {
    return 'Hello ${name}';
}

// function and => do the same thing here
// you will use arrow functions 90% of the time in react