const user = {name: 'Levi', age: 21};
console.log(user.name)

const people = ["Aki", "Rin", "Kai"];
console.log(people[1]);

// use destructuring often

const { name1, age1 } = user;
const [first, second] = people;

